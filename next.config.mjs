/** @type {import('next').NextConfig} */
const nextConfig = {
  // Any other Next.js specific configurations would go here.
  // DO NOT add the 'webpack' section for resolving aliases here,
  // as jsconfig.json handles that and causes 'require is not defined' error.
};

export default nextConfig;
